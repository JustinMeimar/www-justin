// place files you want to import through the `$lib` alias in this folder.
export type Project = {
    title: string,
    date: string,
    desc: string, 
    route: string, 
    imgLink: string
};   